interface:
  display_name: "Paper Scheduler"
  short_description: "Schedule paper topics for a future digest cycle"
  default_prompt: "Use $paper-scheduler to schedule, review, or cancel a future paper selection plan."
